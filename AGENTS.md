# 多Agent智能虚拟化实验平台项目规则

本文件是本项目的项目级规则。通用编码、安全、文件编辑和 Codex 工作流规则继续继承上级规则；本文件只记录本项目的固定事实、技术边界和交付约束。

## 1. 权威事实与变更原则

**文档检索口径**：本仓库的权威文档根目录为 `documents/`（2026-09-17 由 `Docs/` 改名，仓库内一律使用新路径），按用途分 `Preparation/`（需求与立项附件）、`Guideline/`（基线章节 `01`–`10` 与 PRD）、`Development/`（启动分析产物与设计资产）三段存放；**不按外部 `documents/第一批文档/`、`documents/第二批文档/` 逻辑路径重建目录，也不移动既有文件**。基线文档中出现的 `documents/第一批文档/...`、`documents/第二批文档/...` 是外部逻辑路径，只用于表达优先级，不等同于本仓库路径。定位任何权威事实前，按「**本节优先级表 → `documents/Guideline/01`~`10` → `documents/Preparation/附件1`**」顺序检索。**`documents/文档索引.md` 不补建（2026-09-14 决定，原 `Docs/文档索引.md`）**：凡以它为「唯一检索入口」「映射表」或 D 编号登记册的引用一律失效，不要再新增对该文件的依赖；检索入口即本节，D 编号登记见 `README.md`「已知缺口与待修项」。

| 优先级 | 权威内容 | 本仓库文件 | 状态 |
|---|---|---|---|
| 1 | 业务需求最高优先级基线（需求变更说明） | — | 已取消（2026-09-17 决定）：不再要求补入；冲突裁决以行 2–5 为序 |
| 2 | 功能范围与验收点，共 **142 条功能记录** | `documents/Preparation/附件1：三级功能清单.xlsx` | **已入库（2026-09-14复核）**：五列结构 `端/页面/一级/二级/三级`，共 142 条；矩阵和 PRD 采用 F001-F142 进行映射，D2 关闭 |
| 3 | 页面、交互与可访问性（v1.0） | `documents/Preparation/附件3：UI设计文档.docx` | 已入库，定版 v1.0（2026-09-17 全仓统一版本号，D4 关闭） |
| 4 | 字段、索引、外键与事务约束（v1.0） | `documents/Preparation/附件5：数据库设计文档.docx` | **已入库（2026-09-17 复核）**：MySQL 8.0 库 `agent_virtual_lab`，44 表 + 4 视图 + 3 组触发器（5 个 CREATE TRIGGER 对象）+ 3 存储过程，含 `team_memberships`；定版 v1.0（2026-09-17 全仓统一版本号，D3 关闭） |
| 5 | 开发实施口径（第三批 v1.0 章节，`01`–`10` + PRD + README） | `documents/Guideline/01`~`10`、`documents/Guideline/PRD.md`、`README.md` | 已入库 |

- **版本口径（2026-09-14 统一、2026-09-17 全仓统一，全仓唯一）**：第三批基线章节 `01`–`10`、`PRD.md`、数据库设计文档（`附件5`）、UI 设计文档（`附件3`）的版本号统一为 **v1.0**（2026-09-17 决定：文档有版本号出现的地方都统一为 v1.0；D3、D4 定版关闭，D10 容量已复核关闭）。`11_页面原型驱动补充需求.md` 曾为页面原型驱动补充章节，已随该章停用（2026-09-17）不再作为基线。各文档原有的候选/待审核状态与立项评审流程不变，仅版本号统一。版本线统一、不得混用；`README.md`、`documents/Guideline/*`、`documents/Development/DESIGN/*` 必须与本口径一致，**不得再出现 v0.3/v0.4/v0.5/v1.1/V4.0/v3.3/v3.2/V2.1/V2.0 等其它版本号（已降级历史资产自身的冻结版本标注除外）**。
- 发生冲突时按上表优先级处理；`10_需求追踪矩阵.md` 必须覆盖三级功能表全部 142 条功能记录所属领域，不得存在未映射的 P0 项。**优先级 2 的 142 条清单已入库，追踪矩阵的 F 编号回溯校验已于 2026-09-14 执行并通过**（142 个编号无缺失、无重复，功能域/页面/三级功能和功能点与清单层级一致）。清单后续如有变更，必须重新生成并复核。
- 背景与业务依据文档（不参与冲突裁决）：`documents/Preparation/附件2：需求调研报告.docx`、`附件4.1：用例设计文档.docx`、`附件4.2：需求分析报告.docx`、`附件4.3：项目立项报告.docx`。`附件4.1` 旧版（2026-9-2，18 条用例）含旧基线内容（实验大厅、浏览/选择模板并自建环境、AI 智能配置/自定义配置、在线编码判题），与 v1.0 基线冲突，**不得以其为用例来源**；顶层 `附件4.1：用例设计文档.docx` 即按 v1.0 基线重写版（18 条用例覆盖 AC-01~AC-15，标注 F 编号与接口路径），**已于 2026-09-17 停用、不再作为测试输入**（D15 关闭，见 README 登记册），旧版作为历史留档。
- **`documents/Preparation/` 盘点（2026-09-14 核实、2026-09-17 复核）**：`附件1`（xlsx 实测 143 行 = 1 表头 + **142 条**功能记录）、`附件2`/`附件4.1`/`附件4.2`/`附件4.3`（背景依据，其中 `附件4.1` 含旧基线用例，见上条）、`附件3`（UI；文档编号 `20260908-01-01`、日期 2026-09-08，定版 v1.0；其原型已含「我的团队（加入团队）」与「团队管理（教师视角）」，与成员资格页面口径一致，但角色称谓仍写「一级/二级管理员」，与四角色编码不统一）。**`附件5`（数据库设计文档，2026-09-14）即 v1.0 字段基线**（44 表 + 4 视图 + 3 组触发器（5 个 CREATE TRIGGER 对象）+ 3 存储过程，含 `team_memberships`；D3 关闭）；**`附件6`（接口设计文档，2026-09-14）为接口设计合成资产**（80 个 API：认证 4 + 用户端 30 + 教学端 21 + 管理端 25，F001-F142 全覆盖）。`附件6` 成员接口角色（§10.1-10.5 全节：查询成员/待审申请/审核加入申请/直接添加/移除成员）已于 2026-09-17 按「学生使用不能创建实验」原则修正为仅 `ORG_ADMIN`（与 `02`/PRD/`05` 角色基线一致，D12 关闭）；管理端监控接口端点（`/admin/health`、`/admin/metrics/overview`、`/admin/alerts`、`/admin/alerts/{id}/acknowledge`，F126-F129）以 `附件6` 为实际口径（D13 关闭）。字段基线与接口基线分别以数据库设计文档 **v1.0** 和 `documents/Guideline/05_接口设计与集成规范.md` 为准。
- **文档口径统一记录（2026-09-10）**：此前本章直接引用外部 `documents/...` 路径，与本仓库实际文件不一致；现统一为「以本仓库 `documents/` 为准」（2026-09-17 由 `Docs/` 改名）——工作路径一律引用仓库内真实文件，并按前述检索口径（本节优先级表 → `documents/Guideline/01`~`10` → `documents/Preparation/附件1`）定位；冲突优先级仍按上表执行（第 1 行已取消，实际以行 2–5 为序）。上表已无缺口（2026-09-17：行 1 取消、行 3/4 定版 v1.0），其他待裁决项以 **D 编号**登记：**登记册固定为 `README.md`「已知缺口与待修项」**（原设想的 `documents/文档索引.md` 第 4 节登记册不再补建），当前无有效条目（D15 已关闭：`附件4.1` v1.0 重写版已停用、不再作为测试输入，旧版仍作背景留档）；D1、D3、D4、D10、D12、D13、D14、D15 已关闭（D1 需求变更说明不再要求补入、D3 数据库设计文档定版 v1.0、D4 UI 设计文档定版 v1.0、D10 12 周容量已复核、D12 附件6 成员接口角色已修正为仅 `ORG_ADMIN`、D13 监控端点已按 `附件6` 实况确认、D14 章节 11 原型文件不再登记、D15 附件4.1 v1.0 重写版停用）；新增或关闭缺口的唯一入口是该表，改完同步本行。`organization_id` 已按产品决策统一改语义为 `team_id`（见 `documents/Development/DESIGN/AGENT_DB.md` 变更记录；该文件已于 2026-09-17 按数据库设计文档 **v1.0** 同步重建为活动资产——13 张表、字段/索引/外键以附件5 v1.0 为权威，M6 Agent 域数据模型按此建表；原 v1.1（2026-09-10）及更早版本已冻结为历史留档，不作为 v1.0 基线依据）。
- **本次基线切换记录**：本项目文档基线已由旧基线（Vue 3 + TypeScript + Spring Boot + FastAPI + LangGraph、学生/老师/超级管理员三类角色、219 功能点、Docker 单节点运行时）切换为 v1.0 基线。凡涉及“三大角色”“Vue 3”“Spring Boot”“FastAPI 主实现”“用户自选模板/镜像/依赖”“Docker 单节点为主实现”“课程/班级体系”等旧描述均已失效，不得在实现中沿用。
- 不得把已确定的实验类型、角色、状态机、Agent 能力、记录归档、安全防护或资源生命周期要求改写为“暂不支持”“后续可选”或从范围中删除。分阶段交付只表示实施顺序，不表示需求范围缩减。
- **章节自检与人员口径（2026-09-14 复核、2026-09-17 统一）**：`10_需求追踪矩阵.md` 第 4 节列有 R1–R4 评审记录，最新一轮为「Guideline 全量口径对齐」。**团队口径固定为「1 名项目经理、3 名后端开发、1 名测试兼运维」**：章头团队配置与责任人列统一为「项目经理 / 后端 1/2/3 / 测试运维」，M7/M8 前端切片按域就近承担，不设专职前端、不记录具体人员姓名与兼任安排。现 `AGENTS.md` §8、`07`、`09`、`PRD` 四份人员口径一致；**任一处再出现「1 名前端」「前端只有 1 人」或具体人员姓名/兼任安排即为口径回归**，须立即修正。
- **接口章节一致性（2026-09-14 双向补齐、2026-09-17 复核）**：`05_接口设计与集成规范.md` 与 `PRD.md` 的接口章节描述同一套接口契约，**以 `05` 为接口基线**（路径、方法、错误码的唯一权威来源），`PRD.md` §9 为整合镜像。两侧缺项已互补：`05` §6 增补成员资格错误码 `403 MEMBERSHIP_REQUIRED`/`MEMBERSHIP_PENDING`、`409 MEMBERSHIP_CHANGE_BLOCKED`/`LAST_TEAM_ADMIN_PROTECTED`。**2026-09-17 复核**：README 原记载的「`PRD.md` §5.4 增补 `POST /admin/instances/{id}/restart`、`GET /admin/metrics/alerts`、`POST /admin/metrics/alerts/{id}/ack`（F126-F129）」为错误记载，已删除；F126-F129 监控/告警接口以 `附件6`（`/admin/health`、`/admin/metrics/overview`、`/admin/alerts`、`/admin/alerts/{id}/acknowledge`）为实际口径（D13 关闭）。**新增或修改任何接口与错误码必须同时更新 `05`、PRD、`附件6` 三处**，禁止只改一侧。
- **UI 约束一致性（2026-09-14 补齐）**：`02_角色权限与业务流程.md` §8 与 `PRD.md` §6（页面与交互要求）为同一套 UI 与可访问性约束，**必须逐条等价**（原「PRD §2.8」为 README/AGENTS.md 记载的节号，与 PRD 实际章节结构不符，统一为 PRD §6）。两侧已于 2026-09-14 对齐，其中「我的团队」页（成员状态、申请处理说明、退出阻断事项，以及非 `ACTIVE` 成员任务页的资格原因与团队入口）与「团队成员」页（仅 `ORG_ADMIN` 可见、状态筛选、申请审核、按账号直接添加、移除确认显示阻断与最后管理员保护）为成员资格专有约束，**任一侧新增或修改条目必须同时更新另一侧**，禁止只改一侧或压缩合并条目。
- **页面原型驱动补充需求（2026-09-17 入库、2026-09-17 停用）**：`documents/Guideline/11_页面原型驱动补充需求.md` 原为页面原型驱动补充章节（2026-09-11，曾纳入 v1.0 统一版本线），以 `UI设计/index.html` 交互原型为输入，收敛页面范围（PUI-01~PUI-11）、页面状态与交互口径、原型收敛决策和验收（PUI-AC-01~08）。**该章节已与 `附件4.1` v1.0 重写版一同停用（2026-09-17 决定），不再作为基线或验收依据**；页面验收以 `附件3`（UI 设计文档 v1.0）与 `02` §8/PRD §6 为准。
- 产品范围、数据保留策略、技术架构或安全边界发生变化时，先记录变更及影响，再修改下游代码、测试和文档；不要通过修改本规则文件来掩盖未确认的产品决策。

## 2. 产品定义

多Agent智能虚拟化实验平台是一套面向教学、训练和实验实践的 PC Web 云实验平台。教师基于已发布且不可变的模板版本创建任务并指定用户；平台自动创建容器或虚拟机环境；用户在浏览器工作区完成实验、保存过程并提交报告或成果；教师或评测服务审核；平台在完成成果归档后，撤销访问、销毁实例并释放配额。

“实验任务”和“实验实例”必须区分：实验任务是教师基于已发布且不可变的模板版本创建、并指定到具体用户的任务定义；实验实例是用户基于自己获分配的任务启动的独立运行环境，必须由有效的 `experiment_task_assignment` 触发。实例之间必须隔离。

平台不是模板市场、通用云主机控制台、在线刷题平台或无约束聊天工具。普通用户不需要安装本地运行时，也不能自行选择镜像、虚拟机模板、依赖、网络策略或资源规格；普通用户只能进入分配给自己的任务，不能创建模板或自由创建环境。

团队成员资格（`team_memberships`）与账号、固定角色分离：`USER` 可申请加入或退出，`ORG_ADMIN` 可审核、直接添加或移除成员，`ORG_SUB_ADMIN` 默认无成员管理权限。**只有 `ACTIVE` 成员能被指定任务**，任务发布与任务启动都必须重新校验成员资格；退出或移除必须先通过阻断检查并撤销该成员的全部工作区会话。

任何路径都不能跳过审核：报告必交任务执行 `finish`（提前结束）时先保存草稿和必要快照，停止交互环境后仍停留在 `REPORT_DRAFT`；非必交任务执行 `finish` 时冻结系统成果清单版本并进入 `UNDER_REVIEW`。平台为单团队，不建立课程、班级、训练营等二级组织体系。

## 3. 用户与角色

平台采用固定四角色，不做自定义角色或自定义权限：

- **`USER`（普通用户）**：申请加入团队、查看本人成员状态、在无阻断事项时退出；查看本人任务、启动/取消装载、使用本人工作区、保存和提交报告/成果、使用受控 Agent、查看审核与归档；禁止创建模板、发布任务、编辑环境方案、查看他人任务、管理成员或运行时。
- **`ORG_SUB_ADMIN`（助教/授权教师）**：在授权任务范围内发布、查看过程、审核和导出；禁止审核/添加/移除成员、修改全局配置、越权访问用户、管理固定角色。
- **`ORG_ADMIN`（教师/团队管理员）**：审核加入申请、直接添加/移除成员；管理模板版本、发布/撤回任务、仅指定 `ACTIVE` 用户、审核、查看授权成果；禁止管理运行时底座和全局安全策略。
- **`SYSTEM_ADMIN`（系统管理员）**：管理账号、固定角色、管理员交接、运行时、镜像/虚拟机模板、配额、补偿、安全和审计；禁止代替教师形成业务审核结论、绕过审计修改成果。

前端路由和按钮只负责可用性，不能替代授权。后端每次读取或写入必须同时校验角色、数据范围、资源归属、任务状态、时间窗口和操作权限，不可仅凭资源主键返回数据。

Agent、策略网关、工具网关、模型服务、容器运行时、资源调度服务和评测服务是系统内部参与者，不等同于平台用户角色。

## 4. 固定业务范围

### 4.1 团队成员资格（单团队）

- 平台为单团队，不建立课程、班级、训练营等二级组织体系。账号、固定角色与成员资格三者分离：`USER` 可提交加入申请或在无阻断事项时退出；`ORG_ADMIN` 可审核通过/拒绝、按账号直接添加或移除成员；`ORG_SUB_ADMIN` 默认无成员管理权限。
- 成员状态固定为 `PENDING`、`ACTIVE`、`REJECTED`、`EXITED`、`REMOVED`。`PENDING` 由用户申请产生；`ORG_ADMIN` 审核通过转为 `ACTIVE`，拒绝转为 `REJECTED`；`ORG_ADMIN` 直接添加可直接创建 `ACTIVE` 记录。
- **仅 `ACTIVE` 成员可接收新任务分配**：任务发布筛选 `ACTIVE` 成员，任务启动在持久化实例事实前重新校验 `ACTIVE`；`PENDING`、`REJECTED`、`EXITED`、`REMOVED` 均拒绝分配与启动。
- 退出与移除必须先检查未完成任务、活动实例以及待审核/归档/销毁流程；存在任一项时返回阻断清单且不变更成员状态。成功退出或移除时，同一事务内追加成员历史、撤销该成员全部 `ACTIVE` 工作区会话、写通知/审计/Outbox。
- 最后一名有效 `ORG_ADMIN` 受保护，只能先由 `SYSTEM_ADMIN` 完成管理员交接，之后才可退出或被移除。
- 成员资格校验由身份与授权模块以领域服务对外提供，前端路由、按钮和本地状态不参与授权判定；成员历史与成员变更记录只追加，不物理删除。

### 4.2 模板与不可变版本

- 教师创建模板草稿，配置任务说明、运行时类型（`CONTAINER` 或 `VIRTUAL_MACHINE`）、运行环境、依赖与启动、资源规格、实验手册、起始资源、评测规则和 Agent 范围。
- 版本发布前必须执行预检（必填、运行时、依赖、材料和健康检查）；只有通过预检的模板版本才能被任务引用。
- 模板版本发布后不可原地修改，已发布版本受引用保护；新变更通过创建新版本表达。

### 4.3 任务发布、指定用户与撤回

- 任务固定引用一个已发布模板版本；发布时一次性固化用户、时间、报告和销毁策略，并写通知 Outbox。
- 支持指定用户、发布预览、幂等发布和按状态撤回；**只能指定 `ACTIVE` 成员**，发布时写入成员关系快照，任务启动时重新校验成员资格。
- 撤回进入 `CANCELLED`：禁止新启动；装载中的任务先取消装载并清理资源；运行中的任务按撤回策略保存草稿和快照、撤销会话并回收；已进入审核、归档或完成的记录不物理删除，只追加撤回/关闭审计事件。

### 4.4 双运行时自动装载与工作区

- 用户任务自动装载容器或虚拟机实例，覆盖镜像/模板、材料、依赖、健康检查和短期凭据。
- 支持阶段进度、SSE 续传、断点重试、取消和幂等；重复启动或页面刷新只复用同一实例，不创建第二个实例。
- 工作区按模板提供 WebIDE、Notebook、Web 终端、Web 预览或浏览器远程桌面，访问全部经过平台网关和短期会话。

### 4.5 报告与成果

- 报告默认必交（`report_required=true`），教师可在系统上限内设为非必交；必交任务提交时冻结报告版本、附件、快照、日志和产物。
- 非必交任务由系统在用户结束时生成系统成果清单版本（manifest 含快照、运行结果、日志和产物），仍需按审核策略审核、归档后销毁。
- 报告自动保存只更新当前可编辑草稿及修订标识；版本提交后正文、附件关联和快照清单不可修改。

### 4.6 审核

- P0 支持人工（`MANUAL`）、自动（`AUTO`）和复合（`COMBINED`）三种审核模式；同一提交版本只能形成一条最终审核记录。
- 退回必须产生新的可编辑版本，历史版本只读；并发审核返回状态冲突和最新结论，不得覆盖。

### 4.7 归档与销毁

- 审核通过后立即触发归档；归档文件包括通过版本正文、附件、快照、日志、评测结果和必要产物，每个文件保存 SHA-256，manifest 保存整体摘要。
- 只有归档状态为 `COMPLETED` 且清单 SHA-256 校验通过，才可撤销访问并销毁实例；归档校验完成后 10 分钟内触发访问撤销和实例销毁。
- 销毁链路先撤销会话、再停止/删除运行时、最后释放配额；失败进入补偿任务或人工处置。

### 4.8 Agent 受控辅助

- Agent 在列表、装载、工作区和报告阶段可达，入口为 P0；上下文绑定、基础问答/诊断、高风险审批和审计均为 P0。
- Agent 只能访问当前授权任务、实例、手册、文件和日志；写删文件、安装依赖、联网和高风险命令均进入策略审批与审计。
- Agent 自动修复、复杂多 Agent 工作流和增强报告生成为 P1 受控辅助能力，不占用 P0 交付资源。

### 4.9 运维与可靠性治理

- 系统管理端提供用户、固定角色、运行时、镜像/虚拟机模板白名单、实例、配额、补偿、事件和审计能力。
- 长耗时工作使用 Celery 异步任务和 SSE 事件；数据库事务只覆盖本地事实状态、Outbox 和审计，不等待运行时操作完成。
- 通知由 Outbox 消费生成；补偿和事件按幂等键/事件 ID 去重；任务超限进入 `MANUAL`，不得无限重试。

## 5. 核心业务流程与数据边界

标准流程为：用户申请加入或教师直接添加 -> 成员成为 `ACTIVE` -> 教师创建模板草稿 -> 环境/依赖/材料预检 -> 发布不可变模板版本 -> 创建任务并仅指定 `ACTIVE` 成员 -> 发布任务并生成通知 -> 用户进入任务 -> 自动装载容器或虚拟机 -> 签发工作区短期会话 -> 用户实验和持续保存 -> 提交报告或系统成果版本 -> 自动/人工/复合审核 -> 通过或退回重提 -> 固化报告、附件、快照、日志、评测和产物 -> 校验归档摘要 -> 撤销全部访问会话 -> 销毁运行时 -> 释放配额 -> 完成。

- **团队成员资格状态机**：`PENDING -> ACTIVE -> EXITED`、`PENDING -> REJECTED`、`ACTIVE -> REMOVED`；只有 `ACTIVE` 可接收新分配，退出/移除受阻断检查与最后一名有效 `ORG_ADMIN` 保护约束。
- **用户流程聚合根为 `experiment_task_assignments`**，承载任务状态、失败阶段、提交、审核和完成时间。`experiment_instances` 只能通过分配记录创建，**禁止以用户 ID 加模板 ID 绕过任务创建实例**。
- **用户任务状态机**：`ASSIGNED -> PROVISIONING -> READY -> IN_PROGRESS`；必交任务 `IN_PROGRESS -> REPORT_DRAFT -> UNDER_REVIEW ->（REJECTED -> REPORT_DRAFT | APPROVED）`；非必交任务 `IN_PROGRESS -> UNDER_REVIEW ->（REJECTED -> IN_PROGRESS | APPROVED）`；`APPROVED -> ARCHIVING -> DESTROYING -> COMPLETED`。
- **实例/报告/归档/会话状态**：实例 `REQUESTED -> PROVISIONING -> READY -> RUNNING -> STOPPED -> DESTROYING -> DESTROYED`（含 `FAILED`）；报告 `DRAFT -> SUBMITTED -> UNDER_REVIEW -> APPROVED/REJECTED`（退回生成新 `DRAFT` 版本）；归档 `PENDING -> ARCHIVING -> COMPLETED`（含 `FAILED` 重试）；访问会话 `ACTIVE -> EXPIRED/REVOKED`。
- **失败与取消**：异步阶段失败时聚合状态进入 `FAILED`，同时保存 `failure_stage`、`previous_status`、错误码、脱敏原因、`retryable`、重试次数和 `trace_id`；超过最大自动重试进入人工处置，不得无限重试。
- **统一标识**：跨模块记录统一携带 `assignment_id`、`instance_id`、`request_id`、`trace_id`、`operation_id`。
- **数据边界**：成员资格、模板主体/版本、教师任务、用户分配、运行实例、报告版本、审核、归档和文件必须分离；文件正文入 MinIO，数据库只存元数据、归属、状态、哈希、引用和审计索引。
- 业务事实只存 MySQL，Redis 只用于锁、缓存、短期会话状态、限流和队列，不得作为任务、报告、审核、配额或成果的唯一事实来源；不得在数据库事务内等待运行时操作完成。

## 6. 技术实施边界

第一阶段冻结技术基线：

- **前端**：React 18 + TypeScript 5 + Vite，由 OpenAPI 生成客户端类型，页面按 UI 设计文档 **v1.0** 实现响应式和可访问性（D4 关闭）。
- **业务 API**：Python 3.12 + Django 5 + Django REST Framework，单一主业务框架，`drf-spectacular` 生成 OpenAPI 3.0。
- **关系数据库**：MySQL 8.0，字段基线为数据库设计文档 **v1.0**（`附件5` 已在库：库名 `agent_virtual_lab`，44 表 + 4 视图 + 3 组触发器（5 个 CREATE TRIGGER 对象）+ 3 存储过程，含 `team_memberships`；D3 关闭）。
- **缓存/队列**：Redis 7 + Celery 5。
- **对象存储**：MinIO，兼容 S3 API。
- **事件流**：SSE，使用 `event_id`、`Last-Event-ID` 续传和轮询降级。
- **运行时**：Docker Engine/OCI 容器与 libvirt/KVM 虚拟机双运行时，统一经 `RuntimeAdapter` 契约访问；业务服务不直接耦合 Docker、Kubernetes、libvirt 或云厂商 SDK。
- **架构形态**：模块化单体 + 独立 Worker；模块接口、数据所有权、事务边界和异步边界必须明确。
- **一致性**：Outbox + 补偿任务；发布任务、报告保存/提交、审核、归档状态变化和会话撤销在同一业务事务内写 Outbox；运行时部分成功使用补偿任务最终收敛。
- **可观测性**：JSON 结构化日志 + Prometheus 指标 + OpenTelemetry trace，以 `assignment_id`、`instance_id`、`request_id`、`trace_id` 联检。

`RuntimeAdapter` 契约包括 `validate_spec`、`create`、`get_status`、`get_progress`、`issue_access`、`snapshot`、`stop`、`destroy`、`collect_metrics`。`spec` 必须由服务端组合生成，不接受普通用户提交的镜像、虚拟机模板、依赖、资源规格、路径或网络规则；适配器必须返回稳定的错误码、`retryable`、外部资源 ID、阶段进度和能力信息。

契约方法固定为上述九个；新增或更名必须在 `03`、`05`、`PRD` 与契约测试中同步。`validate_spec` 与 `create` 必须接收并校验幂等键，`destroy` 同样以幂等键保证重复销毁可收敛。

- **测试工具**：pytest、Django test、Playwright、k6、OWASP ZAP，统一纳入 CI 质量门禁。
- **部署与环境**：开发用 Docker Compose（Web、API、Worker、MySQL、Redis、MinIO、Fake/Container Adapter，可重复启动与本地契约联调）；测试与生产同拓扑（API、Worker、网关、双 RuntimeAdapter、对象存储与监控），执行 E2E、性能、安全和恢复演练；预发布/UAT 使用受控容器与 KVM 资源池、脱敏种子数据、真实凭据服务和告警；生产按 Web/API、Worker、网关、MySQL、Redis、MinIO、容器池、KVM 池分层部署。生产最小备份为 MySQL 全量/增量、MinIO 版本与生命周期配置、代码/镜像清单和关键密钥引用；微服务拆分须有独立扩缩容、隔离或发布需求作为变更依据。

**后续扩展（不得与主路径混用）**：FastAPI、PostgreSQL、Kubernetes/KubeVirt、RQ/Arq 和 WebSocket 属于后续适配或扩展项，不进入第一阶段主实现；不得在同一代码路径混用 Django/DRF 与 FastAPI，也不得未经审批用 PostgreSQL/FastAPI/Celery 替代方案替换主实现。

## 7. 安全硬约束

- 身份认证使用短期访问令牌（数据库只保存 JTI 摘要）和可轮换刷新令牌；服务端从令牌解析用户，不信任客户端 `user_id`、`role`、`assignment_id` 的归属声明。
- 采用固定 RBAC 并叠加数据范围校验；后端每次读写必须校验角色、成员资格、数据范围、资源归属、任务状态、时间窗口和操作权限。仅 `ACTIVE` 团队成员可被分配或启动任务；成员退出/移除成功后必须立即撤销该成员的全部工作区会话。
- 容器禁止 root、特权模式、宿主路径挂载和 Docker Socket；限制 CPU、内存、磁盘、进程数和执行时长；默认拒绝网络并受控挂载。
- 虚拟机使用白名单模板和 Cloud-init 最小化配置；访问凭据短期有效且可撤销；镜像与虚拟机模板在使用前经过白名单和漏洞扫描。
- 网络规则、依赖源、文件路径、命令、插件和模型调用采用白名单、参数校验和必要确认；上传文件执行类型、大小、扩展名、内容扫描和对象摘要校验。
- 审计、事件、版本和补偿记录只追加，修正通过新事件表达，不允许删改历史；日志、Agent 上下文、终端输出和错误响应必须脱敏，不得输出令牌、云密钥、SQL、内部地址或堆栈。
- 审核通过前不得销毁唯一报告、快照、关键日志和成果；归档摘要未校验不得进入销毁。
- 运行时、Agent、对象存储和数据库管理凭据不得下发至浏览器；密码、API 密钥、工作区令牌、远程桌面密码只使用密钥服务或短期令牌，不写入业务表明文。

## 8. 人员配置与职责

项目团队共 5 人（2026-09-10 项目经理确认、2026-09-17 口径统一）：**1 名项目经理、3 名后端开发、1 名测试兼运维**，不设专职前端，前端页面由三名后端按域切片承担。

- 1 名项目经理：需求确认、计划、风险、跨角色协调、版本验收和文档管理。
- 3 名后端开发：按三条责任流分工，并按域切片承担 M7（用户端）与 M8（教学/系统管理端）前端页面。
- 1 名测试兼运维：CI、种子、Fake、E2E、k6、ZAP、监控告警、备份恢复和发布门禁（M9）。

三条责任流（含各自域的前端切片）：

- **后端 1**：`M1 工程基础与身份授权 → M4-B 工作区网关与访问会话 → M6 Agent 与策略网关`；前端切片：用户端登录/任务入口、云端工作区页面、Agent 悬浮面板。
- **后端 2**：`M2 模板、预检与教学任务 → M5 报告、评测、审核与归档`；前端切片：教学端模板/任务页面、用户端报告与成果页、审核页面。
- **后端 3**：`M3 实验编排与容器运行时 → M4-A 虚拟机运行时`；前端切片：系统管理端运行时/实例/配额/补偿/审计页面。

M7/M8 前端切片跟随各责任流的联调窗口交付，原按专职前端设定的页面排期不再适用；12 周总工期容量已于 2026-09-17 复核通过（缺口 D10 关闭；登记册见 `README.md`「已知缺口与待修项」）。职责可以在计划阶段细分，但每个阶段必须明确唯一负责人、验收人、测试证据和运行方式，不能默认“后端”或“测试”承担未分配的系统责任。

## 9. 开发与交付规则

- 采用主干开发或短生命周期功能分支；一个模块任务对应一个可审查变更集，不混入无关重构。
- 所有数据库变化使用 Django migration；提供空库迁移、升级迁移、回滚/恢复说明，禁止应用启动隐式改表。
- **G1 契约冻结前只做骨架/Mock**：G1 冻结前只开发工程骨架、页面壳、Mock 和测试夹具（含 Fake RuntimeAdapter、OpenAPI、事件 Schema）；G1 通过后进入业务联调。
- **需求追踪矩阵先行**：需求、状态机、API、数据库、UI 或验收变化必须先更新第三批章节和 `10_需求追踪矩阵.md`，再改代码；矩阵覆盖三级功能表全部 142 条功能记录所属领域，每个 F 编号必须能追溯到唯一来源行。
- 配置只从受控配置和密钥服务读取；禁止提交密码、令牌、云密钥、真实个人数据、生产导出和未脱敏日志；新依赖必须记录版本、用途、许可证、镜像来源和漏洞扫描结果；OpenAPI、JSON Schema、数据库迁移和测试 fixture 同步评审。
- **接口通用约定（依据 `05`）**：统一前缀 `/api/v1`、OpenAPI 3.0；JSON 使用 UTF-8，文件走受控预签名 URL；幂等键适用于所有会改变事实或触发外部动作的接口（成员申请/审核/直接添加/退出/移除、任务发布、启动与取消、报告保存与提交、成果提交、审核、提前结束、归档、会话撤销、销毁和补偿重试），服务端至少保留 24 小时，重放返回原 HTTP 状态和响应体，键与请求摘要不一致返回 `IDEMPOTENCY_CONFLICT`；列表统一返回 `data.items`、`page`、`page_size`、`total`、`request_id`，排序与过滤字段白名单化；写操作支持 `If-Match`/`ETag` 或显式 `revision`，并发冲突返回 409；异步操作返回 `operation_id` 与 `trace_id`；错误响应不泄露内部地址、凭据、SQL、堆栈或他人资源是否存在。
- **质量门禁**（合入前最低要求）：

| 变更类型 | 合入前最低要求 |
|---|---|
| 业务逻辑/状态机 | 单元测试覆盖正常、越权、状态冲突、重试、取消和部分成功 |
| 成员资格 | 申请、审核、直接添加、退出、移除、非 `ACTIVE` 启动拒绝、会话撤销、阻断事项和最后管理员保护 |
| API | OpenAPI 契约、鉴权、参数、幂等、ETag/revision、错误码和事件测试 |
| 数据库 | 空库/升级迁移、外键/唯一约束、事务回滚、备份恢复验证 |
| RuntimeAdapter | Fake、容器、虚拟机三类适配测试；创建、进度、访问、快照、停止、销毁幂等 |
| 文件/归档 | 上传扫描、摘要校验、预览、导出权限、归档清单和失败补偿 |
| 前端页面 | 主流程、冲突、断线恢复、桌面/窄屏视觉和可访问性检查 |
| Agent/安全 | 策略拒绝、高风险审批、上下文隔离、工具审计、依赖和敏感信息扫描 |
| 运维 | 指标、告警、Outbox/补偿、回滚、数据库与对象存储恢复演练 |

- **禁止项**：
  - 普通用户接口接受镜像、虚拟机模板、资源规格、宿主机路径、任意命令或运行时提供方。
  - 通过前端路由、隐藏字段、调试接口绕过任务分配、版本冻结、审核或销毁前置条件。
  - 将运行时、Agent、对象存储或数据库管理凭据下发至浏览器。
  - 在同一代码路径混用 Django/DRF 与 FastAPI，或将 PostgreSQL/FastAPI/Celery 替代方案无审批带入主实现。
  - 无限重试、无幂等外部调用、无事件去重的通知/事件消费者。
  - 审核通过前销毁唯一成果，或删改审计、事件、报告和归档历史。
- 代码、测试和文档使用中文业务术语与稳定英文标识混合命名；接口字段、事件字段、数据库列和日志字段保持一致，优先使用 `assignment_id`、`instance_id`、`request_id`、`trace_id`、`operation_id` 等明确标识。
- 生成的缓存、预览目录、临时脚本和测试产物只在本次任务期间保留；交付前清理本次产生且不属于交付结果的文件。

## 10. 验收重点

第一阶段 P0 业务验收场景 AC-01、AC-01A、AC-01B、AC-02~AC-15：

| 编号 | 场景 | 通过标准 |
|---|---|---|
| AC-01 | 登录和权限 | 四角色路由正确；`USER` 页面和直接 API 均无模板写入、发布、运行时配置和他人数据权限 |
| AC-01A | 团队加入与成员状态 | 未加入 `USER` 可提交申请且重复 `PENDING` 幂等；`ORG_ADMIN` 可审核通过/拒绝或按账号直接添加；`ORG_SUB_ADMIN` 成员接口被拒绝；仅 `ACTIVE` 成员可被新分配或启动 |
| AC-01B | 团队退出与教师移除 | `USER` 退出、`ORG_ADMIN` 移除均二次确认并检查未完成任务、活动实例、审核/归档/销毁流程；阻断清单可见；成功后会话撤销、通知、审计和历史保留；最后有效 `ORG_ADMIN` 受保护 |
| AC-02 | 模板预检与任务发布 | 只能选择已发布版本；预检失败不可发布；指定用户、时间、报告和销毁策略一次性固化；重复发布不重复分配/通知 |
| AC-03 | 任务撤回 | 撤回禁止新启动；装载/运行实例按策略保存、撤销、回收；已审核/归档记录保留且有通知和审计 |
| AC-04 | 容器自动装载 | 按冻结模板完成镜像、材料、依赖、健康检查和短期凭据；重复点击只产生一个实例 |
| AC-05 | 虚拟机自动装载 | 完成白名单模板、Cloud-init、系统盘、远程桌面/Web 终端、快照和销毁；启动时间和失败路径符合指标 |
| AC-06 | 装载进度与恢复 | 阶段/步骤/耗时/百分比/失败原因/`retryable`/`trace_id` 可见；SSE 断线用 `Last-Event-ID` 续传且不重复；可断点重试/取消 |
| AC-07 | 工作区和会话 | 手册、起始模板、结果区和正确工作区可用；WebIDE/Notebook/终端或远程桌面按模板展示；会话到期/撤回不可继续访问 |
| AC-08 | 工作区和报告保存 | 工作内容和报告自动保存 ≤10 秒；断线恢复；草稿、附件、预览、版本历史和 ETag 冲突均可验证 |
| AC-09 | 报告/成果提交 | 必交任务冻结报告版本、附件、快照、日志和产物；非必交任务生成系统成果 manifest；重复提交幂等，不可伪造证据 |
| AC-10 | 审核和退回 | 人工/自动/复合三种模式至少各有一条用例；通过/退回唯一且可审计；退回产生新版本，历史不可覆盖；并发审核返回冲突 |
| AC-11 | Agent 可控性 | 列表/装载/工作区/报告均可通过鼠标/键盘打开；上下文只含授权对象；高风险安装/写删/联网/命令必须审批并记录调用 |
| AC-12 | 通知 | 发布、装载失败、提交、审核、归档、销毁和撤回通知可读/已读；跳转前鉴权，目标失效不泄露信息 |
| AC-13 | 归档 | 通过版本、附件、快照、日志、评测、产物全部进入归档；manifest 和文件 SHA-256 校验通过；归档未完成不能销毁 |
| AC-14 | 销毁和配额 | 先撤销会话，再停止/删除容器或虚拟机，最后释放配额；重复销毁、运行时已删数据库失败和补偿最大次数均能收敛/转人工 |
| AC-15 | 审计与 Outbox | 可按 assignment/instance/trace 查询完整时间线；业务事务与 Outbox 原子；消费者去重；日志不含明文凭据 |

**上线阻断条件**（任一不满足即不可上线）：

- 存在用户可创建未分配实例、查看他人任务、绕过审核或访问已撤销工作区的路径。
- 存在非 `ACTIVE` 成员获得新任务分配、启动已分配实验或签发工作区会话，或退出/移除可绕过阻断、审计、会话撤销和最后 `ORG_ADMIN` 保护的路径。
- 容器或虚拟机任一 P0 运行时无法完成创建、访问、快照、销毁和失败收敛。
- 重复启动、回调、提交或补偿会产生多个实例、重复扣配额、覆盖版本或销毁唯一成果。
- 归档摘要和文件校验未完成却进入销毁，或归档/销毁失败没有补偿/人工入口。
- Outbox/通知/SSE 无法去重和续传；关键动作无法通过业务 ID 和 trace 查询。
- 报告附件、预览、冲突保存、非必交成果、任务撤回或 Agent 高风险审批未通过。
- 未达到并发、启动、回收、可用性、RPO/RTO 和存储安全余量基线。
- 安全、备份恢复、虚拟机隔离或销毁故障恢复演练未完成。

任何实现如果只完成页面或接口而没有可运行的实验闭环，不能视为该阶段完成。
