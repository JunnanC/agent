"""Celery 入口（doc 02 §一 ``tasks.py``）。

P03-A 刻意**没有**异步任务：课程、任职、grant、选课审批与移除的写路径
全部是同步事务（doc 02 §六 的事务边界表在本阶段没有异步条目），
把能同步完成的事拆成 Celery 只会引入「响应说成功但事实还没落库」的窗口。

P03-B 的批量名册导入与课程复制会在这里落 job 任务。它们必须只调用
service/port，不复刻业务判断（doc 00 §3.2、doc 06 §一）。
"""
