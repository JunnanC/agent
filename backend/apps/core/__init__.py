"""Domain-kernel contracts shared by all backend applications.

The core package is deliberately framework-light.  It owns contracts and
invariants that must remain stable while Django/DRF adapters evolve.
"""
