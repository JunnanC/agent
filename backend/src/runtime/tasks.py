"""Celery task discovery entry for the runtime app."""

# Import concrete @shared_task functions from runtime.extend.tasks here when
# they are introduced. The current extend module contains task abstractions.
