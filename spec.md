# 本地开发环境 Spec

## 背景

项目需要一套可在空机器上重复启动的本地环境，用于支撑「教师立实验 → 学生跑容器虚拟机 → 提报告 → 审核归档」样例链路的开发。当前仓库尚未提供统一的编排、配置拆分、密钥防误提交和基础健康检查能力。

技术栈冻结为 Python 3.12 + Django 5 + DRF + drf-spectacular + MySQL 8.0 + Redis 7 + Celery 5 + MinIO + Docker Compose。代码兼容性下限为 Python 3.11+；格式化与检查统一使用 ruff，不使用 black。

## 目标

- 空机器克隆仓库后，仅需复制环境变量示例文件并执行一条启动命令，即可获得可用的 MySQL、Redis、MinIO、API、Worker 与 Beat。
- 本地环境不依赖开发者本机手工安装服务或手工修改文件。
- 敏感信息与运行数据不入仓。
- MySQL、Redis、MinIO 的存储职责严格分离。

## 功能需求

- F1: 提供 Compose 编排，包含 MySQL 8.0、Redis 7、MinIO、API、Worker、Beat 六个服务。基础服务具备持久化命名卷与健康检查；API、Worker、Beat 必须等待基础服务健康后启动。
- F2: MySQL 使用 utf8mb4 与 InnoDB，并显式采用 UTC 连接时区，避免时间字段漂移。
- F3: Redis 7 开启 AOF 持久化，并按逻辑库隔离用途：db 0 用于缓存、锁、限流、短期会话状态与幂等记录；db 1 专用于 Celery broker 与 result backend。
- F4: MinIO 控制台端口与 API 端口分离，数据持久化，并至少创建四个私有桶：普通文件、实验成果与归档、运行时快照、导出物。桶名通过环境变量集中配置。
- F5: 文件访问必须通过服务端签发的有期限预签名 URL；业务 API 不代理文件正文。审计导出必须使用独立导出桶。
- F6: Django 配置拆分为 base/dev/test/prod，敏感值与环境差异值统一从环境读取；集中配置模块负责类型转换与必填校验，缺少必需变量时启动即失败。
- F7: 提供根目录、后端、前端三份环境变量示例文件。每项均写明用途、是否必填，并使用明显为假的占位值。
- F8: 提供根 `.gitignore`，覆盖环境与密钥、Python、Node、本地设施、IDE/OS 产物，并确保环境变量示例文件不被忽略。
- F9: 提供 API 存活健康检查端点，只读返回自身与 MySQL、Redis、MinIO 的连通性，不暴露凭据、内部地址或详细错误堆栈。
- F10: 提供一键启动与停止脚本，并允许后续加入 E3 的 Fake/Container Adapter 服务骨架；该骨架默认注释，不实现业务逻辑。
- F11: 后端 README 记录环境变量清单与本地环境设计决策；新增环境变量必须同步更新清单。

## 非功能需求

- N1: 不提交 `.env`、证书、私钥、云密钥、API key、真实个人数据或生产导出。
- N2: 不依赖 PostgreSQL、Kubernetes、FastAPI、RQ/Arq、WebSocket 或云厂商运行时，不保留备选主路径。
- N3: 启动流程可重复执行；停止后再启动不丢失数据库、Redis 持久化数据或对象数据。
- N4: 基础服务未健康时，应用服务不得提前启动。
- N5: 代码兼容 Python 3.11+，并通过 `ruff format --check` 与 `ruff check`。
- N6: 配置读取职责集中在后端配置模块，业务代码不得散落直接读取环境变量。

## 不做的事

- 不修改数据库表结构、字段、约束或新增表。
- 不实现业务功能、UI 页面、鉴权体系或业务 API。
- 不实现 Fake/Container Adapter 的内部逻辑。
- 不生成或执行 Django migration；模型仍由数据库结构基线管理。
- 不提供生产部署方案，只提供本地开发环境。
- 不重写 Git 历史；若发现已入库真实凭据，先上报并作废轮换，再由组长决定后续处理。

## 验收标准

- AC1: 在空机器上克隆仓库、复制环境变量示例文件、执行启动脚本后，健康检查端点返回正常状态，期间无需手工修改文件。
- AC2: 仓库中不存在 `.env`、密钥、证书或真实凭据文件；仅保留 `.env.example`。
- AC3: 实际连接 MySQL 后，可验证数据库字符集为 utf8mb4、表引擎为 InnoDB，且连接时区为 UTC。
- AC4: Worker 可启动并消费一个任务；清理 Redis db 0 不影响 db 1 中的 Celery 队列。
- AC5: MinIO 创建四个私有桶，并能通过预签名 URL 完成一个小文件的上传与下载。
- AC6: 缺少任一必需环境变量时，应用启动失败并给出明确错误；不得使用占位串静默降级。
- AC7: 所有新增环境变量均登记在后端 README 清单中，包含用途、必填性、默认值与影响范围。
- AC8: `ruff format --check` 与 `ruff check` 均通过。
- AC9: 启动脚本可一键启动，停止脚本可一键停止；基础服务数据在停止后再启动仍保留。

## 需要确认的决策

- C-ENV-1: 生产环境是否使用同一个 Compose 文件扩展，还是仅由运维另行提供生产编排？本阶段默认只承诺本地开发环境。
