# common 模块 Spec

## 背景

平台按 M1~M6 模块化建设，common 是所有模块复用的底座，必须先于业务模块交付。现有接口与数据库规范已经冻结：统一响应、错误码、请求追踪、分页、幂等、Outbox、审计等横切能力需要集中实现，避免各业务模块自行造轮子或产生重复语义。

common 只提供与具体业务无关的基础能力。判断标准是：这些能力拿出当前项目后，仍可被另一个无关项目复用。

## 目标

- 为所有 API 提供统一响应、错误码、请求标识、追踪标识与结构化日志。
- 提供安全分页、Redis 幂等回放、Outbox 事务性发布、统一审计记录与审计查询导出能力。
- 通过抽象契约隔离身份与权限实现，避免 common 反向依赖业务模块。
- 以测试证明关键横切行为，包括幂等、分页、错误处理、日志脱敏、事务回滚与审计导出。

## 功能需求

- F1: 统一响应成功、列表、异步受理与错误四种信封格式。成功 `code` 与错误 `code` 均为数字；成功固定为 `0`，错误采用接口文档数字错误码；每个响应都必须带 `request_id`。
- F2: 提供唯一错误码字典，集中维护数字错误码、符号名、HTTP 状态、默认消息与触发条件。业务模块只能引用该字典，不得自造错误码。数字码为对外协议；符号名仅作为内部常量别名，便于代码阅读与检索。
- F3: 提供请求标识与追踪标识能力。入站请求可生成或透传 `request_id`，响应头与日志必须包含该值；异步任务与 Outbox 链路必须携带 `trace_id`。
- F4: 提供结构化 JSON 日志，至少包含 `request_id`、`trace_id`、用户标识、动作与耗时。日志出口必须统一执行敏感信息脱敏，禁止明文输出密码、Token、Cookie、Authorization 头与 API 密钥。
- F5: 提供请求上下文存取能力，使业务代码不需要层层传参即可读取当前 `request_id` 与 `trace_id`。
- F6: 提供统一分页能力，入参支持页码、页大小、排序字段与排序方向，出参包含数据项、页码、页大小与总数。页大小默认 20、最大 100，超限自动截断；排序字段必须白名单校验；默认排序必须稳定，翻页不得重复或丢失数据。
- F7: 提供 Redis 通用幂等能力。幂等键由用户标识、接口范围与请求 `Idempotency-Key` 组成；记录至少保存 24 小时，内容包括请求摘要、执行状态、原 HTTP 状态码与原响应体。摘要一致时回放原响应；摘要不一致时返回幂等冲突；并发请求只能执行一次业务。
- F8: 幂等能力以可选装饰器方式提供给需要写操作的视图启用，不全局强制；GET、HEAD、OPTIONS 不要求幂等。业务表级幂等约束由各业务模块自行实现。
- F9: 提供与业务事实同一事务写入 Outbox 的发布能力，作为唯一合法跨模块副作用通道。事件记录必须包含唯一事件 ID、事件类型、聚合类型与 ID、主题、脱敏载荷、状态、尝试次数、可用时间、追踪标识与创建时间；载荷必须脱敏并包含事件标识、事件类型、发生时间、追踪标识及场景关联 ID。
- F10: 提供 Outbox 投递基础设施，只负责发布与投递状态维护，不消费任何具体业务事件。业务事实回滚时，同一事务中的 Outbox 记录必须一并回滚。
- F11: 提供统一审计记录能力，写入操作者、角色、动作、目标类型与 ID、关联资源、请求标识、追踪标识、幂等键、结果、原因、变更前后快照、脱敏 IP 与用户代理摘要。成功、拒绝与失败都必须落审计。
- F12: 提供系统管理员只读审计检索能力，支持分页、多维筛选、关联资源查询与白名单排序，默认按创建时间倒序。
- F13: 提供系统管理员审计导出能力。导出必须幂等，支持 CSV 与 JSON、指定脱敏字段，返回异步受理信息；完成后提供文件地址、大小、记录数、脱敏字段与过期时间。导出文件必须存放在独立导出桶，有效期 24 小时；导出动作本身必须落审计。
- F14: 提供身份与权限的抽象契约，由身份模块延迟加载实现，用于判断用户能否执行动作及数据范围，并从请求解析可审计操作者。common 不得导入或依赖任何业务模块。
- F15: 提供基础鉴权角色规则，支持不同角色对个人、教学与管理接口的访问范围判断，具体身份识别由抽象契约实现。

## 非功能需求

- N1: common 不得包含业务语义，不得导入任何业务应用；代码中不得出现成员、任务、报告、实例、模板等业务词。
- N2: common 只允许直接读写 `outbox_events`、`audit_logs` 与 Redis 幂等记录，其余表一律不得直接读写。
- N3: `audit_logs` 为仅追加表，禁止更新和删除；修正历史只能新增一行表达。审计数据默认保留 180 天。
- N4: 所有时间语义使用 UTC，保留数据库规范要求的精度。
- N5: 所有持久化字段、状态与约束必须严格符合 V4.0 数据库基线，不新增表、不修改表结构、不生成替代表。
- N6: Outbox 载荷、审计原因、审计快照、日志与导出内容必须统一脱敏；不得泄露凭据、Token、Cookie、Authorization 头、API 密钥或用户代理原文。
- N7: 统一错误处理必须保证任意异常返回约定错误信封，不返回 HTML 错误页，不泄露堆栈。
- N8: 核心测试覆盖率不低于 80%，并通过 `ruff format --check` 与 `ruff check`。

## 不做的事

- 不实现任何业务功能、业务 API、业务事件消费或业务数据模型。
- 不为业务模块新增或修改幂等唯一约束，不代行业务表级幂等。
- 不修改 V4.0 冻结表结构，不新增幂等表。
- 不再以符号码作为对外错误协议。
- 不实现文件扫描、病毒检测、对象存储桶生命周期管理或生产部署能力。
- 不直接实现身份认证、用户资料与具体权限规则，只提供抽象契约与通用角色规则。

## 验收标准

- AC1: 成功、列表、异步受理与错误响应均符合规定信封；成功 `code` 为 `0`，错误 `code` 为文档指定数字码，响应包含 `request_id`。
- AC2: 数字错误码与 HTTP 状态映射逐条断言通过；业务代码无法绕过统一错误码字典；数字码与符号名映射表存在且可维护。
- AC3: 中间件为每个请求生成或透传 `request_id`，响应头、上下文与日志均能读取；异步任务与 Outbox 记录携带 `trace_id`。
- AC4: 抽样日志包含全部结构化字段，且不出现密码、Token、Cookie、Authorization 头或 API 密钥明文。
- AC5: 分页测试覆盖默认值、最大值截断、非法排序拒绝、翻页无重复与无丢失。
- AC6: 同一幂等键并发请求只执行一次业务，后续请求回放原状态码与响应体；不同摘要返回 409；24 小时后记录失效。
- AC7: Outbox 与业务事实在同一事务写入；业务回滚时 Outbox 记录同步消失；载荷字段与脱敏要求满足规范。
- AC8: 成功、拒绝与失败审计均落库；仅追加约束下更新或删除审计会失败；多条件检索、关联查询与默认排序正确。
- AC9: 审计导出文件落入独立导出桶，有效期 24 小时；超上限返回指定错误；空筛选返回指定错误；导出动作落审计。
- AC10: common 不导入任何业务模块；通过依赖检查确认没有业务词和越界表访问。
- AC11: 覆盖率不低于 80%，`ruff format --check` 与 `ruff check` 通过。

## 待裁决

- C13 已裁决：对外错误协议采用第 10、15 章的数字错误码；第 24 章符号名仅作为内部常量别名与映射键。
